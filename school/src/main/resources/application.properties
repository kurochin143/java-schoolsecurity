server.port=${PORT:2019}
#Feature that determines what happens when no accessors are found for a type
#(and there are no annotations to indicate it is meant to be serialized).
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=none
#use .sql file at resources = always
spring.datasource.initialization-mode=never
# For h2 database
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
server.error.whitelabel.enabled=false
# For PostgreSQL local
#spring.datasource.url=jdbc:postgresql://localhost:5432/schooldb
#spring.datasource.username=postgres
#spring.datasource.password=${MYDBPASSWORD:admin}
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
# For PostgreSQL
spring.datasource.url=${SPRING_DATA_URL:jdbc:postgresql://${VAR_HOST}/${VAR_DATABASE}?user=${VAR_USERNAME}&password=${VAR_PASSWORD}&sslmode=require}
spring.datasource.username=${VAR_USERNAME}
spring.datasource.password=${VAR_PASSWORD}
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true